1. docker 컨테이너 서버 서비스 기술 문서 (가안)

    1.1 전산설비실 할당 ip 대역
        - 100.1.1.0/24
        - Gateway : 100.1.1.1

    1.2 규칙
        1.2.1
            docker 명명규칙:
                * docker image name:
                    - mzc-image-{service}-{role}
                    - ex) mzc-image-ftp-logBackup

                    - 키밋 이미지 사용시 (docker_id)/mzc-image-{service}-{role}
                    - ex) dpcalfola/mzc-image-ftp-logBackup
                * docker container name:
                    - mzc-{service}-{role}-{number}
                    - ex) mzc-ftp-logBackup-1
                * docker network name:
                    - mzc-network-{service}-{role}
                    - ex) mzc-ftp-logBackup-network
                * docker volume name:
                    - mzc-volume-{service}-{role}
                    - ex) mzc-ftp-logBackup-volume

        1.2.2
            컨테이너 옵션
                * restart:
                    - always
                * port:
                    - 보안상 외부 포트는 랜덤값으로 설정하고 내부 포트는 기본값 사용
                    - 외부 포트는 각 서비스 문서에 기술
                * volume, network
                    - 원칙적으로 1개의 role당 1개의 volume, network 사용
                    - 상호 연결이 필요한 경우 volume과 network를 공유하며 이때 명칭은 {role}을 제외하고
                    {service} 만 사용

    1.3 문서 분류
        1.3.1
            도커 엔진 설정 문서
                * Filename: docker-container-option.txt
                * network, volume 등의 컨테이너 생성 이전에 기 입력해야 하는 명령어를 추합하여 작성
        1.3.2
            개별 서비스
                * Directory 단위로 작성
                *
                * 포함 내용
                    - 의존성 설정 명령어 (도커 엔진 설정 문서의 개별 내용 분리 작성)
                    - 실행 명령어

